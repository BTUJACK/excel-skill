

VLOOKUP函数介绍：
=VLOOKUP(要查找的内容、要查找的位置、包含要返回的值的范围内的列号、返回表示为 1/TRUE 或 0/FALSE 的近似或精确匹配项)。

使用技巧：
必须把要查找的对象放在第一列，而且数字必须是数字格式；
必须把要查找的范围固定住，否则，使用下拉菜单，使用范围就变小了。固定方法是，选中范围后按下F4按钮，或者手动在范围每个字母前面添加$字符；
=IFERROR(VLOOKUP(B3, '2'!$B$3:$G$131, 6,0), "N")


IFERROR函数介绍：
IFERROR(value, value_if_error)
IFERROR 函数语法具有下列参数：
value    必需。 检查是否存在错误的参数。
value_if_error    必需。 公式计算结果为错误时要返回的值。 评估以下错误类型：#N/A、#VALUE！、#REF！、#DIV/0！、#NUM！、#NAME？或 #NULL！。


实战公式：
=IFERROR(VLOOKUP(B3, '2'!$B$3:$G$131, 6,0), "N")介绍：
我的项目是从两个sheet中查找相同的物料号。
该公式是项目的实际公式。
该公式位于sheet1中，重命名为了1；
另外一个sheet命名为2；
B3是要查找的对象；
2是指从sheet2中查找；
!$B$3:$G$131是从B3到G131的范围中查找；
6是指返回该列对应的内容；6指的是从B列到G列的G列；如果想返回第6列的内容，这个参数就填写6；
0只精准查找；

"N"表示IFERROR函数如果没有找到对象返回一个字符N；


